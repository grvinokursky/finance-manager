# Итоговый проект "Сервис управления финансами"

## Описание

Данный проект представляет backend-приложение для управления личными финансами. Проект позволит пользователям добавлять доходы и расходы, устанавливать бюджеты на категории и получать статистику по своим финансам.

Взаимодействие с пользователем осуществляется через интерфейс командной строки.

## Быстрый старт

1. Убедитесь что на вашем компьютере установлен JDK 25.
2. Скачайте и распакуйте на своем компьютере архив с приложением.
3. Из директории архива выполните команду `java -jar "finance-manager-1.0-SNAPSHOT.jar"`

Альтернативный вариант запуска приложения через IntelliJ IDEA.

Для быстрой проверки был добавлен пользователь и набиты данные для него. Данные для входа: логин `grvinokursky` - пароль `1f2f3f`.

## Управление

### Авторизация

После запуска приложения вам потребуется выполнить авторизацию. 

Доступные команды:

- `registration (сокращенно reg) <username> <password>` - Регистрация пользователя (длина пароля должна быть не менее 6 символов).
- `login (сокращенно l) <username> <password>` - Авторизация пользователя.

### Сервис управления финансами

После авторизации будет запущен сервис управления финансами. 

Доступные команды:

- `create-income-category <categoryName>` - Добавить категорию доходов.
- `create-expenses-category <expensesCategoryName> <limit>` - Добавить категорию расходов, требуется указать лимит по расходам (лимит должен быть целым числом).
- `add-income-operation <categoryName> <value>` - Добавить операцию доходов.
- `add-expenses-operation <categoryName> <value>` - Добавить операцию расходов.
- `show-all-info` - Отобразить общую статистику.
- `show-info-by-income` - Отобразить статистику по доходам.
- `show-info-by-expenses` - Отобразить статистику по расходам.
- `show-info-by-categories: <categoryName1> && <categoryName2> ... && <categoryNameN>` - Отобразить статистику по выбранным категориям (перечислите категории через символы '&&').
- `exit` - Выход из аккаунта.

При вводе копеек в денежных значениях в качестве разделителя используйте точку.

## Описание решения

### Архитектура решения

При проектировании решения было предусмотрено разделение на следующие слои:

- слой данных,
- слой сервисов,
- слой приложения.

#### Слой данных

Слой данных включает модели данных, контейнер для хранения данных и репозитории для инкапсуляции взаимодействия с хранилищем данных.

#### Слой сервисов

Данный слой включает в себя сервисы, заключающие бизнес-логику приложения. Также для ответов сервиса управления финансами были созданы модели отчетов.

#### Слой приложения

Слой приложения включает пользовательское CLI-приложение.

### Система сборки

В качестве системы сборки используется Gradle.

Решение использует Spring Framework.

### Модели данных

При проектировании моделей данных было решено создать отдельные модели для категорий расходов и доходов, а также для операций доходов и расходов.

Данный способ оказался удобен, а также предоставляет большую гибкость при внесении расширений, в том случае если будут предоставлены новые требования.

Что же касается использования паттерна репозиторий, он поможет в том случае, если будет решено перейти от хранения данных в файле к хранению в БД.

### Безопасность

В приложении реализовано хеширование паролей при помощи библиотеки BCrypt.